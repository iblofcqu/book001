import numpy as np

# 生成[0,10)的数组，默认步长为1
print(np.arange(10))
# 生成[0,10)的数组，默认步长为1
print(np.arange(0, 10))
# 生成[1,5)的数组，步长为0.5
print(np.arange(1, 5, 0.5))
# 倒序生成(-1,9]的数组，步长为1
print(np.arange(9, -1, -1))
